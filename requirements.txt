# RCA Pocket — Dependências Python
# Instale com: pip install -r requirements.txt

# Dashboard e visualização
streamlit>=1.32.0
plotly>=5.19.0

# Manipulação de dados
pandas>=2.2.0
openpyxl>=3.1.2

# HTTP / Jira API
requests>=2.31.0

# GitHub API (indexação de TAs)
PyGithub>=2.1.1

# YAML
pyyaml>=6.0.2

# Playwright é necessário apenas para sync_jira_browser.py (uso local)
# Instale manualmente se precisar: pip install playwright && python -m playwright install chromium
# playwright>=1.40.0
