# RCA Pocket — Dependências Python
# Instale com: pip install -r requirements.txt

# Dashboard e visualização
streamlit>=1.32.0
plotly>=5.19.0

# Manipulação de dados
pandas>=2.2.0
openpyxl>=3.1.2

# HTTP / Jira API
requests>=2.31.0

# GitHub API (validação de TAs)
PyGithub>=2.1.1

# YAML (já no projeto principal, listado para completude)
pyyaml>=6.0.2

# Browser automation (sync via login manual no Jira — temporário até liberação do token)
playwright>=1.40.0
